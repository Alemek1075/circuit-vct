services:
  circuit:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ConnectionStrings__Circuit: Data Source=/app/data/circuit.db
    volumes:
      - circuit_data:/app/data
    restart: unless-stopped

volumes:
  circuit_data:
