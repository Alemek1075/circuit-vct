# Журнал виконання

## 24–25.09.2026

- Старий застосунок «Місто подій» перенесено за межі робочої папки в `D:\DZ\WEB_IT-archived-2026-09-24`. Масове видалення відхилила автоматична перевірка; новий код створено з чистого шаблону.
- Етап 1: створено .NET 8 MVC + EF Core SQLite, міграцію, моделі `Tournament`, `Team`, `Match`, 14 результатів Paris 2025 і 8 оголошених матчів Shanghai 2026. Джерела в `SPEC.md`. Чиста база створила таблиці та дані після збірки.
- Етап 2: побудовано адаптивні верхню й нижню сітки зі зв'язками, таблицю місць, розклад та сторінки матчу й команди. У браузері вибір NRG підсвітив 4 матчі та 3 зв'язки. Мобільне переповнення виправлено; E2E перевіряє ширину 390 px.
- Етап 3: додано MVC CRUD у `/studio`, JSON CRUD API, валідацію рахунку, захист пов'язаних записів, `skip/limit/nextLink`. `tests/ApiSmoke.ps1` пройшов. Запис обмежено локальним Development режимом.
- Етап 4: додано серверний пошук команд у формі матчу після 3 символів із 250 ms затримкою та клавіатурним вибором, фільтри матчів за етапом/днем, canonical і description для публічних сторінок, `robots.txt`, `sitemap.xml` і `noindex` для редактора. Зовнішній SEO-аудит потребує публічного URL.
- Етап 5: додано read-only SignalR hub, надсилання змін рахунку після збереження API/MVC, оновлення відкритих вкладок без навігації та Chart.js графік різниці карт на сторінці команди. `npm run bench:transport` порівняв WebSockets, SSE і Long Polling на шести однакових змінах; результат у `TRANSPORT_REPORT.md`.
- Етап 6: Dockerfile, Compose, health endpoint і GitHub Actions створено. `docker compose config --quiet` і `dotnet publish -c Release` пройшли. Production збірка локально повернула 200 для сторінки, health і CSS, 404 для `/studio`, 403 для POST/DELETE API. Docker daemon недоступний; реальний запуск контейнера не підтверджено. GitHub Actions після виправлення ігнорування `Data` завершився успішно на коміті `f7ee5c2`; оновлений CI буде перевірено після наступного push.
- Локально `dotnet build -c Release` пройшов без попереджень; `npm run test:e2e`: 10 із 10 сценаріїв.
- Етап 7: OAuth, хмарне розгортання, CD і зовнішній SEO-аудит ще не виконані. Проєкт не називає архівний знімок живими офіційними даними.
