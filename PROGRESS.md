# Журнал виконання

## 24–25.09.2026

- Старий застосунок «Місто подій» перенесено за межі робочої папки в `D:\DZ\WEB_IT-archived-2026-09-24`. Масове видалення відхилила автоматична перевірка; новий код створено з чистого шаблону.
- Етап 1: створено .NET 8 MVC + EF Core SQLite, міграцію, моделі `Tournament`, `Team`, `Match`, 14 результатів Paris 2025 і 8 оголошених матчів Shanghai 2026. Джерела в `SPEC.md`. Чиста база створила таблиці та дані після збірки.
- Етап 2: побудовано адаптивні верхню й нижню сітки зі зв'язками, таблицю місць, розклад та сторінки матчу й команди. У браузері вибір NRG підсвітив 4 матчі та 3 зв'язки. Мобільне переповнення виправлено; E2E перевіряє ширину 390 px.
- Етап 3: додано MVC CRUD у `/studio`, JSON CRUD API, валідацію рахунку, захист пов'язаних записів, `skip/limit/nextLink`. `tests/ApiSmoke.ps1` пройшов. Запис обмежено локальним Development режимом.
- Етап 4: додано серверний пошук команд у формі матчу після 3 символів із 250 ms затримкою та клавіатурним вибором, фільтри матчів за етапом/днем, canonical і description для публічних сторінок, `robots.txt`, `sitemap.xml` і `noindex` для редактора. Зовнішній SEO-аудит потребує публічного URL.
- Етап 5: додано read-only SignalR hub, надсилання змін рахунку після збереження API/MVC, оновлення відкритих вкладок без навігації та Chart.js графік різниці карт на сторінці команди. `npm run bench:transport` порівняв WebSockets, SSE і Long Polling на шести однакових змінах; результат у `TRANSPORT_REPORT.md`.
- Етап 6: Dockerfile, Compose, health endpoint і GitHub Actions створено. `docker compose config --quiet` і `dotnet publish -c Release` пройшли. Production збірка локально повернула 200 для сторінки, health і CSS, 404 для `/studio`, 403 для POST/DELETE API. Docker daemon недоступний; реальний запуск контейнера не підтверджено. Віддалений GitHub Actions `Build and test` завершився успішно на коміті `5091476`.
- Локально `dotnet build -c Release` пройшов без попереджень; `npm run test:e2e`: 10 із 10 сценаріїв.
- Публічний перегляд: `https://alemek1075.github.io/circuit-vct/` опубліковано workflow з `main`. Статичний експорт 48 сторінок локально перевірено на маршрути й ресурси, після публікації перевірено HTTP 200 для головної, Shanghai, команди, матчу, CSS і sitemap; браузерний сценарій пройшов без помилок ресурсів. Це read-only знімок без API, редактора й SignalR.
- SEO-аудит публічного перегляду: Lighthouse 13.5.0 згенерував JSON-звіт без `runtimeError`, мобільна SEO категорія 100/100; деталі й обмеження в `SEO_REPORT.md`. PageSpeed Insights API відповів 429. CLI повернув exit code 1 лише на видаленні тимчасового профілю Edge після створення звіту.
- Етап 7: OAuth і хмарне розгортання динамічного .NET сервера з постійною базою ще не виконані, тому повний CD динамічного застосунку не підтверджено. Проєкт не називає архівний знімок живими офіційними даними.

## Оновлення інтерфейсу 25.09.2026

- Перероблено сторінки двох турнірів: темна сцена події, окремий рядок фіналу, світла сітка й таблиці. Сторінки команд і матчів приведено до тих самих кольорів і типографіки. Активний розділ у верхній навігації змінюється при прокручуванні.
- Усі 21 умовну емблему замінено зображеннями команд з офіційних сторінок VALORANT Esports. Для Shanghai використано фото арени з анонсу Riot Games; README уточнює, що це ілюстрація попереднього турніру в місті.
- `dotnet build Circuit.slnx -c Release --no-restore`: 0 помилок, 0 попереджень. `npm run test:e2e`: 11/11. Статичний експорт створив 48 сторінок; локально перевірено сторінки Paris та Shanghai без помилок завантаження ресурсів, на ширині 390 px без горизонтального переповнення.
